import mongoose from "mongoose";

const ChatSchema = new mongoose.Schema({
    chatName: { type: String, trim: true },
    isGroupChat: { type: Boolean, default: false }, // Distinguish between one-to-one and group chats

    // One-to-One Chat Section
    oneToOneUsers: [
        {
            type: mongoose.Schema.Types.ObjectId,
            ref: "User",
        }
    ],

    oneToOneMessages: [ // Sequential message storage for one-to-one chats
        {
            senderId: { type: mongoose.Schema.Types.ObjectId, ref: "User" }, // Sender
            text: { type: String }, // Message content
            timestamp: { type: Date, default: Date.now }, // Message timestamp
        }
    ],

    // Group Chat Section
    groupUsers: [
        {
            type: mongoose.Schema.Types.ObjectId,
            ref: "User",
        }
    ],
    groupMessages: [
        {
            senderId: { type: mongoose.Schema.Types.ObjectId, ref: "User" }, // Sender
            text: { type: String }, // Message content
            timestamp: { type: Date, default: Date.now }, // Message timestamp
        }
    ],

    groupAdmin: {
        type: mongoose.Schema.Types.ObjectId,
        ref: "User",
    },

    latestMessage: {
        type: mongoose.Schema.Types.ObjectId,
        ref: "Message",
    },
}, {
    timestamps: true, // Automatically add createdAt and updatedAt fields
});

const Chat = mongoose.model("Chat", ChatSchema);

export { Chat };
